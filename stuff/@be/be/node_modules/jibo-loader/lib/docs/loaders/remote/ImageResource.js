    /**
     * Don't use after this
     * @private
     */

    /**
     * Begin the load
     * @private
     */

    /**
     * Called if a load errors out.
     * @private
     */

    /**
     * Called if an abort event fires for request.
     * @private
     */