#!/bin/sh

# Get the default robot's hostname
robotHost=`jibo robot-list|grep '*'|awk '{print \$3 }'`;

# Stops any running skill
echo "Stopping skill...";
ssh root@$robotHost 'pkill -f "skill-main.js" 2> /dev/null';
