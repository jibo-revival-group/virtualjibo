/**
 * @fileOverview
 *
 * Created on 7/6/16.
 * @author Siggi Orn <siggi@jibo.com>
 */
import * as events from 'events';
export interface DOFOptions {
    allOrNothing: boolean;
}
export interface KeysPlaybackOptions {
    jibo: any;
    animPath?: string;
    assetPack?: string;
    animData?: any;
    rootPath?: string;
    cache?: string;
    configBuilder?: Function;
    handleAnimation?: Function;
    screenCenterOverride?: boolean;
    overrideDestroy?: boolean;
    overrideUnload?: boolean;
    ownerInformation?: string;
    dofOptions?: DOFOptions;
    disableSetFaceAnim?: boolean;
}
export declare class KeysPlayback extends events.EventEmitter {
    private _animation;
    private _cache;
    private _alias;
    private _options;
    private _instance;
    private _hasScreenDOFs;
    private _resolver;
    constructor(_animation: any, _cache: string, _alias: string, _options: KeysPlaybackOptions);
    /**
     * Starts playing animation
     * @param {Function} [configBuilder] Configure animation builder
     * @param {Function} [handleAnimation] Returns the animation created by `builder.play()`
     * @param {boolean} [screenCenterOverride] Override the default behavior of
     * centering screen dofs before playing an animation that has any screen content
     * @deprecated
     * @returns {Promise}
     */
    play(configBuilder?: Function, handleAnimation?: Function, screenCenterOverride?: boolean): Promise<any>;
    /**
     * Starts playing animation
     * @returns {Promise}
     */
    play(): Promise<any>;
    _playAndHandleInstance(): void;
    /**
     * Stops playing animation
     */
    stop(): void;
    _cleanup(): void;
}
export declare class PlaybackUtils {
    /**
     * Creates a KeysPlayback object
     * @param {KeysPlaybackOptions} options
     * @returns {Promise<KeysPlayback>}
     */
    static createKeysPlayback(options: KeysPlaybackOptions): Promise<KeysPlayback>;
}
