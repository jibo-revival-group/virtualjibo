export declare class ProcessResult {
    code: number;
    stdout: string;
    stderr: string;
    error: any;
}
export declare class TerminalUtils {
    /**
     * Runs a terminal command
     * @param {string} cmd Command to run
     * @param {string[]} [args] Arguments to command
     * @param {string} [cwd] Working directory to execute command
     * @returns {Promise<T>}
     */
    static run(cmd: string, args?: string[], cwd?: string): Promise<ProcessResult>;
}
