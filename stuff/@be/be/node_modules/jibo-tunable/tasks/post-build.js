module.exports = function(gulp, options, plugins) {
    options = Object.assign({}, options, options.clientOptions);
    gulp.task('post-build', plugins.releaseTask(
        gulp, options, plugins
    )); 
};