/**
 * Manage a set of objects with ids and a cache
 * @type {[type]}
 */
export declare class CacheManager<ObjectType, IDType, CacheType> {
    idToObject: Map<IDType, ObjectType>;
    objectToId: Map<ObjectType, IDType>;
    idToCache: Map<IDType, CacheType>;
    cache: Map<CacheType, Set<ObjectType>>;
    addObjectToCache(obj: ObjectType, id: IDType, cache: CacheType): void;
    getById(id: IDType): ObjectType;
    removeCache(cache: CacheType): ObjectType[];
    /**
     * Removes both the object and the id from cache, but does not
     * delete an entire cache
     * @param {IDType} id
     */
    removeById(id: IDType): ObjectType;
    /**
     * This removes the object and id from cache, but does not delete
     * an entire cache
     * @param obj
     */
    removeByObj(obj: ObjectType): void;
}
