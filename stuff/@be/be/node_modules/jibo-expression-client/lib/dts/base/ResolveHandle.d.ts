import { ClientRemoteObject, RemoteClient } from 'jibo-client-framework';
export declare class ResolveHandle<T> extends ClientRemoteObject {
    promise: Promise<T>;
    result: T;
    private resolvePromise;
    private canceled;
    private resolved;
    constructor(client: RemoteClient, options: {
        instanceId: number;
    });
    cancel(): Promise<void>;
    onPromise(result: T): Promise<void>;
}
