import { ClientRemoteObject, RemoteClient } from 'jibo-client-framework';
export declare class ReleaseHandle<T> extends ClientRemoteObject {
    private released;
    private valToReturnWhenAlreadyReleased;
    constructor(client: RemoteClient, options: {
        instanceId: number;
    }, valToReturnWhenAlreadyReleased: T);
    release(): Promise<T>;
}
