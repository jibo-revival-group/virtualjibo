This package is for sharing MIMs between the on-robot chitchat skill
and the cloud-based GQA service.  Due to on-robot memory constraints,
scripted responses are served from both, but to avoid confusion we
want a single point of truth and a single pile of MIMs for both.
