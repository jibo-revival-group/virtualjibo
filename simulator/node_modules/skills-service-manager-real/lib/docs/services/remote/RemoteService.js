/**
 * RemoteService is for passing messages to the skill process where the RemoteSkill and
 * jibo-command-library lives. This is only because much of what Remote needs to do is in the `jibo`
 * module, not the SSM.
 */